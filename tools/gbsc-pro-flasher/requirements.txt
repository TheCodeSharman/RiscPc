# Runtime dependencies for gbsc_pro_flash.py (for non-Nix / pip users).
# Nix users get these from the repo flake dev shell instead (`nix develop`).
# ymodem pulls in ordered-set transitively.
pyserial>=3.5
ymodem>=1.5
